// Abra o banco de dados
//global.db = sqlite3_open("nome_do_banco.db");

// Verifique se a conexão foi bem-sucedida
//if (global.db == undefined) {
  //  show_error("Erro ao abrir o banco de dados!", true);
//}
